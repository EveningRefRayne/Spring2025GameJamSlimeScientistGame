using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaveManager : MonoBehaviour
{
    //migrated this into FieldManager because they had so much overlap it made more sense to just have them be the same thing.
}
